---
# vars_pulp.yaml
image_name: pulp-operator
# github source
source_repo: pulp/pulp-operator
source_branch: ansible
# build image
build_image_registry: quay.io
build_image_repo: aknochow/pulp-operator
build_image_tag: test
build_image_push: true    
build_image: "{{ build_image_registry }}/{{ build_image_repo }}:{{ build_image_tag }}"
# kubernetes
deploy: true
deploy_namespace: pulp